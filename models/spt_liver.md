# model: spt_liver
Autogenerated ODE System from SBML file with sbmlutils.
```
time: [min]
substance: [mmol]
extent: [mmol]
volume: [l]
area: [m^2]
length: [m]
```

## Parameters `p`
```
PEX_Km_P = 0.5  # [mmol/l] 
S2PT_Km_S = 0.5  # [mmol/l] 
S2PT_Vmax = 0.05  # [mmol/min/l] 
SIM_Km_S = 0.5  # [mmol/l] 
TDETOX_Km_napqi = 0.5  # [mmol/l] 
TDETOX_Vmax = 0.005  # [mmol/min/l] 
T_necrosis_threshold = 5.0  # [mmol/l] 
Vext = 1.5  # [l] 
Vli = 1.5  # [l] 
cf_species_dimensionless = 1.0  # [1/mmol] 
f_metabolism = 1.0  # [-] 
f_porous_media = 0.0  # [-] 
position = 0.5  # [-] 
protein_random = 0.5  # [-] 
zonation_pattern = 0.0  # [-] 
```

## Initial conditions `x0`
```
P = 0.0  # [mmol/l] Vli
P_ext = 0.0  # [mmol/l] Vext
S = 0.0  # [mmol/l] Vli
S_ext = 0.0  # [mmol/l] Vext
T = 0.0  # [mmol/l] Vli
protein = 1.5  # [-] Vli
tsim = 0.0  # [min] 
```

## ODE system
```
# y
PEX_Vmax = S2PT_Vmax * 100  # [mmol/min/l]
SIM_Vmax = S2PT_Vmax * 100  # [mmol/min/l]
f_reaction = (1 - f_porous_media) * f_metabolism  # [-]
necrosis = piecewise(1, T > T_necrosis_threshold, 0)  # [-]
protein = piecewise(1 * 0.5, abs(zonation_pattern - 0) < 1e-6, 1.1406136 * position, abs(zonation_pattern - 1) < 1e-6, 4.552802 * (1 - (1 - position)**10 / ((1 - position)**10 + 0.25**10)), abs(zonation_pattern - 2) < 1e-6, 0.8902508 * (1 - position), abs(zonation_pattern - 3) < 1e-6, 2.3939743 * (1 - position**10 / (position**10 + 0.25**10)), abs(zonation_pattern - 4) < 1e-6, protein_random, abs(zonation_pattern - 4) < 1e-6, protein_random)  # []
PEX = (f_reaction * Vli * PEX_Vmax / PEX_Km_P) * (P - P_ext) / (1 + P_ext / PEX_Km_P + P / PEX_Km_P)  # [mmol/min]
S2PT = f_reaction * (1 - necrosis) * Vli * protein * S2PT_Vmax * (S / (S + S2PT_Km_S))  # [mmol/min]
SIM = (f_reaction * Vli * SIM_Vmax / SIM_Km_S) * (S_ext - S) / (1 + S_ext / SIM_Km_S + S / SIM_Km_S)  # [mmol/min]
TDETOX = f_reaction * (1 - necrosis) * Vli * TDETOX_Vmax * (T / (T + TDETOX_Km_napqi))  # [mmol/min]

# odes
d P/dt = -PEX / Vli + S2PT / Vli  # [mmol/l/min]
d P_ext/dt = PEX / Vext  # [mmol/l/min]
d S/dt = SIM / Vli - S2PT / Vli  # [mmol/l/min]
d S_ext/dt = -SIM / Vext  # [mmol/l/min]
d T/dt = S2PT / Vli - TDETOX / Vli  # [mmol/l/min]
d protein/dt = 0  # [-/min]
d tsim/dt = 1  # [min/min]
```